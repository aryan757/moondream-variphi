version: '3.8'

services:
  app:
    build: .
    container_name: moondream-app
    ports:
      - "8000:8000"
    volumes:
      - ./service-account-file.json:/app/service-account-file.json:ro
    env_file:
      - .env
    restart: unless-stopped 